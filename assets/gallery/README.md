# Folder-driven gallery

Create folders such as:

```text
public/assets/gallery/research/
public/assets/gallery/conferences/
public/assets/gallery/talks/
```

Add media files and optional `folder.json` / `captions.json` files, then run:

```bash
npm run scan:media
```
